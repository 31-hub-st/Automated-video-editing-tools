# Changelog

本文件只记录正式版本。研发过程、旧候选包和 `0.x` 历史细节保留在 Git 提交记录中，不再作为当前产品合同。

## [Unreleased]

### Fixed

- Windows PowerShell 5.1 部署验收脚本在输出 JSON 前显式把 `Generic.List` 转为数组，成功与失败路径都不再因 `@($checks)` 触发 `Argument types do not match`；一键恢复和 bootstrap 因此可以可靠解析最终验收结果。
- 面向普通用户的全新 Hub 一键恢复入口改为正式更新 ZIP 的强制根目录载荷；`一键恢复StoryForge-Hub.cmd`、稳定 ASCII 入口及其复用的部署/验证脚本必须一起进入同一个受校验制品，缺少任一文件时包体门禁失败。
- 换机说明不再要求普通用户下载仓库源码 ZIP；用户从最新稳定 Release 下载正式 `StoryForge-<版本>-update.zip`、完整解压后即可在根目录双击恢复入口。
- Hub 启动时即使正在校验数百 MB 的每日快照，网页健康接口也不再等待备份互斥锁；恢复部署与员工登录可以及时判断 Hub 已上线，完整备份验证仍在后台继续执行。
- Hub 与员工端的连接握手不再通过十四项业务表统计来判断服务身份；有界身份探针和轻量认证 RPC 起源于 `v1.0.5` 候选，当前 `v1.0.6` 延续该合同，部署验证同时检查网页与客户端协议端点，避免正式大库在冷 I/O 或并发校验时把登录拖过超时预算。
- 旧 Hub 启动器修复不再比较计划任务主体的显示字符串；短用户名、`域\用户名` 和 SID 会统一解析为当前 Windows 用户 SID，外来 SID 或无法解析的主体仍安全拒绝，避免同一账号因 Task Scheduler 返回形式不同而被误判。
- 旧 Hub 任务迁移同时严格兼容正式主机的隐藏窗口带引号参数形态和历史简化参数形态；仅接受两种完整合同，混合格式、错序、额外参数、错误路径或端口都会在写入任务前拒绝。

### Release status

- 当前源码候选版本为 `v1.0.6`。真实完整测试、冻结构建、包体哈希和 GitHub 远程门禁尚未完成前不得发布或交付；未填写证据见 [v1.0.6 发布交付报告](docs/V1.0.6_RELEASE.md)。

## [1.0.5] - 2026-08-13

### Release status

- 该版本的完整测试、冻结构建、包体审计和远程门禁均曾通过，但在正式换机验收中发现 Windows PowerShell 5.1 无法稳定序列化部署验收脚本中的 `Generic.List`，会抛出 `Argument types do not match`，使 bootstrap 无法取得可靠的最终验证 JSON。
- 两个 Release 附件下载数均为 `0`。发现问题后 `v1.0.5` 已转为预发布、移出 GitHub `latest` 并停止分发；其标签、附件、哈希和门禁证据只作为历史记录保留，序列化修复纳入 `v1.0.6`。

## [1.0.4] - 2026-08-13

### Release status

- 该版本的完整测试、冻结构建、制品审计和远程门禁均曾通过，但旧 Hub 启动器修复仍按计划任务主体显示字符串比较当前账号；Windows 返回短用户名或 SID 时会把同一账号误判为外来主体并拒绝迁移。
- 两个 Release 附件下载数均为 `0`。发现问题后 `v1.0.4` 已转为预发布、移出 GitHub `latest` 并停止分发；其标签、附件、哈希和门禁证据只作为历史记录保留，SID 规范化修复纳入 `v1.0.5`。

## [1.0.3] - 2026-08-12

### Fixed

- Hub 接收上传失败时会先完成 `.upload` 临时文件清理再返回错误；Windows 文件占用采用有界重试，最终清理失败会记录到 Hub 错误状态，避免客户端收到响应后仍残留临时文件。
- Windows 契约测试固定调用 Windows PowerShell 5.1，隔离不兼容的 `PSModulePath`，并将 8.3 短路径测试目录规范化为长路径，避免 GitHub Windows runner 的环境差异造成误报。

### Release status

- `v1.0.2` 的 GitHub 远程发布门未通过，发布附件下载数为 `0`；该版本已标记为暂缓分发，不作为稳定最新版。上述修复纳入重新构建和验证的 `v1.0.3`。

## [1.0.2] - 2026-08-12

### Added

- 新电脑可通过 GitHub 私有仓库一键安装最新稳定程序，并严格校验 Release digest、外部 manifest 与包内 manifest。
- Hub 固定资料支持发布为唯一的 `hub-state-latest` 私有快照，每次覆盖同名资产，不累计重复备份。
- 新增换机部署、快照发布、部署验收脚本和 Codex 接手规则。
- 制作台为简介卡和口令卡提供各自独立的开关、绝对开始时间和显示时长；口令卡时长设为 `0` 时持续到视频结尾。
- 员工可为单个批次覆盖平台搜索文案和结尾引导文案；覆盖内容随批次冻结，不修改管理员维护的团队平台模板，也不授予 `platforms.manage`。

### Safety

- 恢复始终是用经验证快照整体替换，不会合并或手工改写 SQLite；非空数据目录默认拒绝覆盖。
- 快照不包含员工素材、成品和缓存；Windows DPAPI 保护的 API Key 换机后需重新填写。
- 员工便携版不再迁移旧 Hub 主机资料；冻结程序只有在启动前明确指定固定 DataRoot 时才能成为 Hub。

### Fixed

- Hub 后台网页进程不再占用本机制作服务端口、生产互斥锁或恢复渲染队列，可与同机员工制作服务独立运行。
- Hub 桌面启动器和登录任务固定使用同一正式 DataRoot；员工启动器会清除误继承的 Hub DataRoot。
- 旧 AppData 中的 Hub 角色、访问凭据、设备身份和业务库不会再被复制到发布目录旁的 `StoryForgeData`。

### Compatibility

- 旧草稿和已保存作业缺少新增卡片时间字段时使用兼容默认值继续读取和执行；兼容读取不会回写或改动已经冻结的作业。

## [1.0.1] - 2026-08-11

### Added

- 为文本、配音、字幕、素材预检、渲染、质检和发布阶段增加只写诊断检查点，便于定位员工电脑失败位置；检查点不会被用于跳过生产步骤。
- Hub 与制作电脑增加 RPC 能力清单和字段协商，支持主机与员工端分批升级，并保留旧 Hub 的兼容回退。
- 增加独立主题样式资源及对应的启动自检、Web 静态路由和正式包完整性门禁。

### Changed

- 文本准备缓存改为原子写入，避免断电或异常退出留下半份 JSON。
- 配音候选的稳定选择键纳入严格数据模型并支持旧数据缺省读取，避免候选音色保存时出现字段不兼容。
- 设备能力仍由服务端严格校验；新客户端会按 Hub 公布的字段投影请求，不放宽账号、权限或数据库写入边界。

### Compatibility

- 不改变现有视频生成顺序、FFmpeg 参数、队列租约、取消、重试、原子发布和素材使用记录逻辑。
- 更新包不包含 `StoryForgeData`、小说正文、平台、口令、账号、生产数据库、素材或成品。

## [1.0.0] - 2026-08-04

### Added

- 一页式制作台，支持常规视频生成、仅生成配音和已有配音更换素材三种制作方式。
- Hub、员工本机制作服务、桌面端与网页端协同；共享小说与生产记录，媒体处理留在员工电脑。
- 小说、平台、口令、发布账号、成员、生产预设、批次卷宗和归档记录的统一资料库。
- 可复用的 StoryForge 配音 MP3 元数据与精确字幕索引。
- 核心程序完整包更新、独立语言组件更新、SHA-256 校验、原子安装与回滚。
- Hub 固定资料的窄边界备份、内容去重、恢复前安全快照与三日保留策略。

### Changed

- 正式版本号升级为 `v1.0.0`，当前合同以代码、自动化测试和无版本号活文档为准。
- 常规视频生成只交付完整 MP4；只有“仅生成配音”交付 MP3。
- 视频素材由员工选择本机文件夹，不再按小说题材自动匹配。
- 生产任务采用单重型任务串行执行、资源预检、失败降级和原子发布，优先保证员工电脑稳定。
- 角色精简为管理员和员工；员工可制作、试听、管理个人方案与本机更新，不能修改共享小说、平台和口令。

### Removed

- 独立样片渲染与审批流程；制作台改为 HTML/CSS 即时预览。
- 员工可见的令牌配置流程。
- 旧内置/共享/无所有者制作方案，以及过时的 `V0.*` 发布文档。

### Compatibility

- 旧 `output_mode=video_and_mp3` 枚举名继续保留，但当前语义是“常规视频生成且只交付 MP4”。
- 任何 `0.x` 旧版迁移都应使用 `v1.0.0` 完整包在新目录人工安装，完成真机验收后再清理旧目录。
