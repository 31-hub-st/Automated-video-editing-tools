# Changelog

本文件只记录正式版本。研发过程、旧候选包和 `0.x` 历史细节保留在 Git 提交记录中，不再作为当前产品合同。

## [1.0.0] - 2026-08-04

### Added

- 一页式制作台，支持常规视频生成、仅生成配音和已有配音更换素材三种制作方式。
- Hub、员工本机制作服务、桌面端与网页端协同；共享小说与生产记录，媒体处理留在员工电脑。
- 小说、平台、口令、发布账号、成员、生产预设、批次卷宗和归档记录的统一资料库。
- 可复用的 StoryForge 配音 MP3 元数据与精确字幕索引。
- 核心程序完整包更新、独立语言组件更新、SHA-256 校验、原子安装与回滚。
- Hub 固定资料的窄边界备份、内容去重、恢复前安全快照与三日保留策略。

### Changed

- 正式版本号升级为 `v1.0.0`，当前合同以代码、自动化测试和无版本号活文档为准。
- 常规视频生成只交付完整 MP4；只有“仅生成配音”交付 MP3。
- 视频素材由员工选择本机文件夹，不再按小说题材自动匹配。
- 生产任务采用单重型任务串行执行、资源预检、失败降级和原子发布，优先保证员工电脑稳定。
- 角色精简为管理员和员工；员工可制作、试听、管理个人方案与本机更新，不能修改共享小说、平台和口令。

### Removed

- 独立样片渲染与审批流程；制作台改为 HTML/CSS 即时预览。
- 员工可见的令牌配置流程。
- 旧内置/共享/无所有者制作方案，以及过时的 `V0.*` 发布文档。

### Compatibility

- 旧 `output_mode=video_and_mp3` 枚举名继续保留，但当前语义是“常规视频生成且只交付 MP4”。
- 任何 `0.x` 旧版迁移都应使用 `v1.0.0` 完整包在新目录人工安装，完成真机验收后再清理旧目录。
