# StoryForge Studio v1.0.4 发布交付报告（候选占位）

> **当前状态：阻断，尚未正式发布。** 本文所有 `TODO` 必须由本次 `v1.0.4` 精确源码提交产生的真实测试、构建、哈希和 GitHub 远程运行结果替换。任何 `TODO` 未完成时，都不得创建或发布 `v1.0.4` 正式 Release，不得把候选目录发给员工，也不得声称程序已经稳定可用。

## 本次修正目标

- 正式 `StoryForge-1.0.4-update.zip` 根目录直接包含普通用户可见的 `一键恢复StoryForge-Hub.cmd`，不再要求用户另外下载仓库源码 ZIP。
- 同一受校验制品还必须包含稳定 ASCII 入口 `Restore-StoryForge-Hub.cmd`，以及 `scripts/restore_storyforge_hub_new_machine.ps1`、`scripts/bootstrap_storyforge.ps1`、`scripts/verify_storyforge_deployment.ps1`。
- 一键入口仍只复用现有恢复、Release/manifest/digest/快照校验与部署验证链，不复制第二套恢复逻辑，不放宽全新主机预检，不触碰现有 Hub 或 live SQLite。
- `v1.0.1`、`v1.0.2`、`v1.0.3` 的历史报告、标签与升级起源保持不变；本报告只记录 `v1.0.4` 新制品证据。

## 精确源码身份

| 证据 | 真实结果 |
| --- | --- |
| 构建提交 SHA | **TODO（发布阻断）** |
| `storyforge.__version__` | `1.0.4` |
| 工作树状态 | **TODO：记录构建前 `git status --short`（必须干净）** |
| 构建时间与 Windows/Python 环境 | **TODO（发布阻断）** |

## 测试与静态检查

| 门禁 | 真实结果 |
| --- | --- |
| Python 全量 `unittest discover` | **TODO：测试总数、通过/失败/跳过、日志路径（发布阻断）** |
| 一键恢复契约测试 | **TODO：真实命令与结果（发布阻断）** |
| 更新 ZIP 根目录恢复载荷测试 | **TODO：真实命令与结果（发布阻断）** |
| PowerShell 5.1 parser 检查 | **TODO：文件数与错误数（发布阻断）** |
| Python 编译、`node --check ui/app.js`、`git diff --check` | **TODO：逐项真实结果（发布阻断）** |

## 冻结构建与包体验收

| 证据 | 真实结果 |
| --- | --- |
| 冻结 EXE 启动与版本验证 | **TODO（发布阻断）** |
| 本地 Kokoro/约定 TTS 验证 | **TODO（发布阻断）** |
| 311.05 秒与不少于 600 秒真实稳定渲染 | **TODO（发布阻断）** |
| 构建目录 `package_smoke.py` | **TODO；必须显示恢复载荷 `status: passed`（发布阻断）** |
| 独立解压目录包体冒烟 | **TODO；必须从最终 ZIP 全新解压后运行（发布阻断）** |
| ZIP 安全审计与数据泄漏扫描 | **TODO：条目数、路径/链接/加密/数据库/业务文件计数（发布阻断）** |

独立解压后必须在 ZIP 根目录逐项确认以下非空文件，并由包体冒烟记录结果：

```text
一键恢复StoryForge-Hub.cmd
Restore-StoryForge-Hub.cmd
scripts/restore_storyforge_hub_new_machine.ps1
scripts/bootstrap_storyforge.ps1
scripts/verify_storyforge_deployment.ps1
```

## 制品大小与 SHA-256

下表严禁沿用 `v1.0.3` 或任何中间构建的数值。

| 制品 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `StoryForge Studio.exe` | **TODO（发布阻断）** | **TODO（发布阻断）** |
| `StoryForge-1.0.4-update.zip` | **TODO（发布阻断）** | **TODO（发布阻断）** |
| `StoryForge-1.0.4-update.zip.manifest.json` | **TODO（发布阻断）** | **TODO（发布阻断）** |

还必须记录并核对内部 `storyforge-update.json` 的版本、入口、目录摘要，以及外部 manifest 的文件名、版本、大小和 SHA-256：**TODO（发布阻断）**。

## GitHub 远程门禁与 Release 核对

| 远程证据 | 真实结果 |
| --- | --- |
| 精确发布提交的 `fast-pr` 运行 URL / conclusion | **TODO（发布阻断）** |
| 精确发布提交的手动 `release-stable-gate` URL / conclusion | **TODO（发布阻断）** |
| 注解标签 `v1.0.4` 指向的提交 SHA | **TODO（发布阻断）** |
| `v1.0.4` 标签触发的 `release-stable-gate` URL / conclusion | **TODO（发布阻断）** |
| Release 附件数量、名称、大小与 GitHub digest | **TODO；必须与本报告逐项一致（发布阻断）** |
| GitHub latest 指向 | **TODO；只能在全部门禁成功后设置为 `v1.0.4`** |

## 数据与生产安全声明

- 本次候选准备和隔离验证不得启动、停止、替换或部署当前生产 Hub。
- 不得读取后写入、合并、清理、迁移或手工修改任何 live SQLite；不得把数据库、`.sfbak`、API Key、密码、员工素材或成品写入 Git 或程序 Release。
- `hub-state-latest` 必须保持独立私有预发布通道；程序 Release 不得捆绑业务快照。

上述安全声明的实际审计结果：**TODO（发布阻断）**。

## 普通用户最终只需点击什么

仅在本报告所有阻断项完成且 `v1.0.4` 成为最新稳定 Release 后：在浏览器登录有私有仓库权限的 GitHub 账号，下载 `StoryForge-1.0.4-update.zip`（不是 Source code/源码 ZIP），完整解压，然后双击解压目录根部的：

```text
一键恢复StoryForge-Hub.cmd
```

仅下载程序不能恢复业务数据；入口仍会通过受校验链下载私有 `hub-state-latest` 快照。详细安全边界见 [StoryForge Hub 新电脑一键恢复](ONE_CLICK_HUB_RECOVERY.md)。
