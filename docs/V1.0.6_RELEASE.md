# StoryForge Studio v1.0.6 正式发布交付报告

> **最终状态：已通过并正式发布。** `v1.0.6` 是稳定版和 GitHub `latest`。正式制品严格来自源码提交 `c4a9356912cd9a0c5e835cac07db4b2959f5f282`；本报告是发布完成后的 docs-only 记录，不移动标签、不重建或替换制品。

## 本次交付

- 修复 Windows PowerShell 5.1 部署验收 JSON：成功与失败路径均可解析，`checks` 为数组，不再出现 `Argument types do not match`。
- 正式 ZIP 根目录包含 `一键恢复StoryForge-Hub.cmd`、稳定 ASCII 入口及其复用的恢复、部署、验证脚本。
- 保留旧 Hub 任务 SID/参数安全迁移、轻量健康探针、备份 `ready` / `operational` 和完整 digest/manifest/快照校验链。
- 简介卡、口令卡均有独立开关、绝对开始时间和显示时长；员工可编辑当前批次的平台搜索文案和结尾引导文案。

## 精确源码与本地证据

| 证据 | 真实结果 |
| --- | --- |
| 源码及标签提交 | [`c4a9356912cd9a0c5e835cac07db4b2959f5f282`](https://github.com/31-hub-st/Automated-video-editing-tools/commit/c4a9356912cd9a0c5e835cac07db4b2959f5f282) |
| `storyforge.__version__` | `1.0.6` |
| 构建前工作树 | 干净；构建、测试均绑定上述提交 |
| 环境 | Windows 10 `10.0.19045`、AMD64、Python `3.12.10` |
| 全量测试 | `1105` 项；失败 `0`、错误 `0`、跳过 `2`；`736.949 s` |
| 全量测试证据 | `D:\StoryForgeRelease\1.0.6-final2\TEST_RESULTS\release-final` |
| 正式构建与验收证据 | `D:\StoryForgeRelease\1.0.6-final3` |

## 门禁与冻结程序验收

| 门禁 | 真实结果 |
| --- | --- |
| WinPS 5.1 验收 JSON | 成功路径退出 `0`、失败路径退出 `1`；两者均输出可解析 JSON 数组，且无 `Argument types do not match` |
| SID/旧任务参数迁移 | 当前账号多种显示形式通过；外来主体与错误参数 fail closed、零写入 |
| 一键恢复及包体契约 | 全部通过；中文与 ASCII 入口均委托现有恢复链，不复制或绕过恢复逻辑 |
| Windows PowerShell 5.1 parser | `17` 个 `.ps1`，parser 错误 `0` |
| 其他静态检查 | Python `compileall`、`node --check ui/app.js`、`git diff --check` 全部通过 |
| 冻结启动与版本 | `BUILD_STARTUP_VALIDATION.json`：`ok=true`、`status=passed`、版本 `1.0.6` |
| 本地 Kokoro | `BUILD_KOKORO_VALIDATION.json`：`ok=true`、`status=passed`；`af_heart`、24 kHz |
| 稳定性验收 | `311.05 s / 30 fps / outro on` 与 `600.8 s / 60 fps / outro off` 均通过；`stable_release_gate_passed` |
| 包体冒烟 | 构建目录、cp1252 独立解压、安装暂存目录均 `ok=true`；启动、FFmpeg、恢复载荷、稳定性报告全部 `passed` |
| ZIP 安全审计 | 路径安全、中文入口及委托链通过；无数据库、`.sfbak`、密钥、密码、员工素材、运行日志或成品 |

独立解压后已验证以下文件存在、非空且与源码委托链一致：

```text
一键恢复StoryForge-Hub.cmd
Restore-StoryForge-Hub.cmd
scripts/restore_storyforge_hub_new_machine.ps1
scripts/bootstrap_storyforge.ps1
scripts/verify_storyforge_deployment.ps1
```

## 最终制品

| 制品 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `StoryForge Studio.exe` | `87528318` | `085b89773239260e44630f3dd101d2a27cae97dc329f70fbe82b0d3867523b9a` |
| `StoryForge-1.0.6-update.zip` | `776759428` | `9d3c3ab19f604a035046335c6b004719abf0b784cce851cd25c5f82b9c8f99af` |
| `StoryForge-1.0.6-update.zip.manifest.json` | `318` | `2d94759749a783175157e9491af42aa6d46bad5283cb69db2de7d16de8a99aeb` |

内部 `storyforge-update.json` 为版本 `1.0.6`、入口 `StoryForge Studio.exe`。完整冻结目录受清单约束的文件数为 `7838`、总大小 `1735624858` 字节、bundle manifest SHA-256 为 `398295122713225e2bf07d0ea02bf1fef6559a4e730dabfb297c88daebf43eeb`。

## GitHub 远程门禁与 Release

| 远端证据 | 真实结果 |
| --- | --- |
| `fast-pr` | [run 31638677045](https://github.com/31-hub-st/Automated-video-editing-tools/actions/runs/31638677045)，`success`，提交 `c4a9356...` |
| 手动 `release-stable-gate` | [run 31643090305](https://github.com/31-hub-st/Automated-video-editing-tools/actions/runs/31643090305)，`success`，提交 `c4a9356...` |
| 标签触发 `release-stable-gate` | [run 31644902625](https://github.com/31-hub-st/Automated-video-editing-tools/actions/runs/31644902625)，`success`，提交 `c4a9356...` |
| 注解标签 `v1.0.6` | 精确指向 `c4a9356912cd9a0c5e835cac07db4b2959f5f282`，未移动 |
| [正式 Release](https://github.com/31-hub-st/Automated-video-editing-tools/releases/tag/v1.0.6) | `draft=false`、`prerelease=false`、稳定版且为 `latest` |
| Release 附件 | 恰好 `2` 个：ZIP `776759428` 字节、manifest `318` 字节；GitHub digest 与本报告完全一致；发布初核下载数均为 `0` |

## 生产 Hub 验收与数据安全

- 正式 Hub 已切换到 `D:\StoryForgeHub\App-1.0.6`，进程 PID `24796`；部署验证脚本通过。
- 只监听 `8765`，未监听 `18765`；直接身份健康探针约 `2.5 ms`。
- 正式库只读核对：`27` 本小说、`884` 条生产记录、运行中 `0`，SQLite `quick_check=ok`。
- 备份状态 `ready=true`、`operational=true`；私有 `hub-state-latest` 快照 SHA-256 为 `73f98ba3b2e87d74bb8206f20e6797b102d7ff435448ffcd2e42c977863755eb`。
- 升级未执行 Hub 恢复，未手工编辑、合并、迁移或清理 live SQLite；程序 Release 不含业务快照、数据库或秘密信息。

## 普通用户最终只需点击什么

在浏览器登录有私有仓库权限的 GitHub 账号，从 [v1.0.6 正式 Release](https://github.com/31-hub-st/Automated-video-editing-tools/releases/tag/v1.0.6) 下载 `StoryForge-1.0.6-update.zip`（不是 Source code/源码 ZIP），完整解压，然后双击根目录的：

```text
一键恢复StoryForge-Hub.cmd
```

仅下载最新程序不能恢复业务数据；该入口会通过受校验链下载私有 `hub-state-latest` 快照。详细边界见 [StoryForge Hub 新电脑一键恢复](ONE_CLICK_HUB_RECOVERY.md)。
