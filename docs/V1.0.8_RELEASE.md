# StoryForge Studio v1.0.8 发布交付报告

> **状态：v1.0.8 已完成正式发布、生产 Hub 切换和员工更新分发。** 精确源码提交为 `f4ed8bf0052de32736a92429301e86213ed9d983`，GitHub 注解标签对象 `77b20b184b7111a490d65e2bc8ff93cef0f9df24` 指向该提交；本地标签已校准为同一对象。正式 Release 为 `draft=false`、`prerelease=false`、Latest，且仅含经过验证的 ZIP 与 sidecar。生产 Hub、员工桌面端、Worker 和员工更新清单均已切换至 `1.0.8`；旧 GitHub 程序 Release 对象和本机明确的旧程序副本已经清理，数据目录、Git 标签、提交历史、Actions 证据和 `hub-state-latest` 均保留。

## 本版本范围

- 继承 `v1.0.7` 的完整真实音色目录、独立语速试听、混合卡片时间轴及旧数据兼容。
- 制作台允许员工自由编辑本批“平台口令卡文案”和“结尾引导文案”的完整模板；管理员平台模板作为默认值。
- `{platform}`、`{code}` 必须各保留一次且不能添加未知变量。服务端保存与排队时依据小说绑定和有效口令重新核对、替换真实值，并冻结本批模板与最终解析结果。
- schema 2 草稿中的 `platform_ending_prefix/platform_ending_suffix` 兼容合并为完整结尾模板；已冻结旧任务保持历史结果不变。
- Kokoro 可用性单测通过明确夹具隔离开发机/CI 组件状态；生产音色可用性仍保持缺组件即不可用的 fail-closed 合同。

## 已完成的发布前门禁

- [x] 源码冻结为 `f4ed8bf0052de32736a92429301e86213ed9d983`，构建和门禁开始前工作树干净。
- [x] 完整模板、服务端 token 锁定、旧草稿兼容和 Kokoro CI 夹具的聚焦测试通过。
- [x] 本地完整回归、Windows PowerShell 5.1 parser、Python `compileall`、Node 语法检查和 `git diff --check` 通过。
- [x] 从冻结提交执行 `ReleaseBuild + RequireStableAcceptance + WithLocalAI`，使用独立的 `D:\StoryForgeRelease\1.0.8-final` 构建根。
- [x] 冻结启动、本地 Kokoro、311.05 秒/600 秒真实渲染、Release attestation 和构建目录 package smoke 通过。
- [x] 正式 ZIP 在全新目录独立解压并执行 cp1252 安全控制台 package smoke；五项一键恢复载荷完整。
- [x] 独立复算 ZIP、sidecar、EXE 的大小和 SHA-256；内部版本严格为 `1.0.8`。
- [x] ZIP 路径、条目和内容安全审计通过，未发现 SQLite、`.sfbak`、`StoryForgeData`、API Key、员工素材、成品或生产输出。
- [x] `main`、`fast-pr`、手动 `release-stable-gate` 和标签触发 `release-stable-gate` 均绑定同一源码提交并通过。
- [x] `v1.0.8` 为指向冻结提交的注解标签；发布前 `hub-state-latest` 已刷新并核对为恰好两个资产。

## 发布后核对项

- [x] 创建私有正式 GitHub Release，仅上传已验证的更新 ZIP 与 sidecar；核对 `draft=false`、`prerelease=false`、Latest、资产大小及 digest。
- [x] 按受管原子升级流程切换生产 Hub；未手改 SQLite，并核对精确版本、健康状态、监听进程和业务数据只读计数。
- [x] 升级 Worker 和当前员工桌面端；本机实际运行路径为 `D:\StoryForge-1.0.8-update`，本机 Worker 监听 `127.0.0.1:18765`，操作方确认该版本已可正常使用。
- [x] 将同一正式 ZIP 发布到 Hub 员工更新通道，清单中的版本、大小和 SHA-256 与 GitHub 正式制品一致。
- [x] 清理 `v1.0.0` 至 `v1.0.6` 的旧 GitHub 程序 Release 对象；完整保留所有 Git 标签、提交历史、Actions 证据和 `hub-state-latest`。
- [x] 清理本机明确的旧程序和旧构建副本；旧目录中的 `StoryForgeData`、Hub `Data`、历史恢复数据库和 v1.0.8 正式制品均保留。
- [x] 以 docs-only 提交补记上述真实结果；未移动远端 `v1.0.8` 标签、重建或覆盖同版本制品。

## 精确源码与测试证据

| 证据 | v1.0.8 结果 |
| --- | --- |
| 制品源码提交 | `f4ed8bf0052de32736a92429301e86213ed9d983`；`main` 远端已核对为同一 SHA |
| `storyforge.__version__` | `1.0.8` |
| 构建前工作树 | clean；正式构建根为 `D:\StoryForgeRelease\1.0.8-final` |
| 环境 | Windows 10 `10.0.19045`；Python `3.12.10` |
| 聚焦测试 | 最终覆盖 175 项，全部通过；四个直接模块 174 项通过，修正旧 UI 断言后 `test_ui_contract + test_voice_preview` 90 项复验通过，平台 token 边界 1 项通过；复验集合有重叠，不重复累加 |
| 本地完整回归 | `1163` 项，失败 `0`、错误 `0`、跳过 `2`；unittest `699.934 s`；结果 `OK (skipped=2)` |
| 完整回归证据 | `D:\StoryForgeRelease\1.0.8-final\TEST_RESULTS\release-final`；`summary.json` 总持续 `701.35 s` |
| Windows PowerShell 5.1 parser | `17/17` 通过 |
| Python / JavaScript / diff 静态检查 | `compileall`、Node `--check ui/app.js`、`git diff --check` 均通过 |

聚焦验证使用的核心命令为：

```powershell
.\.build-venv\Scripts\python.exe -m unittest `
  tests.test_library_service `
  tests.test_voice_preview `
  tests.test_api_hub_runtime `
  tests.test_ui_contract -v

.\.build-venv\Scripts\python.exe -m unittest `
  tests.test_ui_contract tests.test_voice_preview -v

.\.build-venv\Scripts\python.exe -m unittest `
  tests.test_library_service.LibraryServiceTests.test_platform_copy_uses_defaults_and_allows_free_text_around_locked_tokens -v
```

## 冻结程序与稳定性证据

| 门禁 | v1.0.8 结果 |
| --- | --- |
| `BUILD_STARTUP_VALIDATION.json` | `ok=true`、`status=passed`、`frozen=true`、版本 `1.0.8` |
| `BUILD_KOKORO_VALIDATION.json` | `ok=true`、`status=passed`、`frozen=true`；`local_kokoro / af_heart` 生成 `3.2 s`、`153644` 字节 WAV |
| `BUILD_STABILITY_ACCEPTANCE.json` | `ok=true`、`stable_release_eligible=true`、`stable_release_gate_passed`；SHA-256 `540d1e364508d459271ae608d1df61ff6338de915a1108399dafb46f85de5988` |
| `BUILD_RELEASE_VALIDATION.json` | `ok=true`、`frozen=true`、`with_local_ai=true`；EXE、启动、Kokoro、稳定性与 bundle 身份已绑定 |
| 构建目录 package smoke | `ok=true`；`23.012 s`；冻结 runtime `exit_code=0` |
| 全新隔离解压 package smoke | `ok=true`；cp1252 安全控制台；`30.113 s`；冻结 runtime `exit_code=0`；版本 `1.0.8` |

| 场景 | 目标 | 实际成片 | 渲染耗时 | 结果 |
| --- | --- | --- | ---: | --- |
| `exact-311s-30fps-outro-on` | `311.05 s`、30 fps、四段素材、BGM、结尾开启 | `311.85 s`、1080×1920、30 fps、H.264、音视频流完整、965 个 ASS 事件 | `227.37 s` | 通过 |
| `sustained-60fps-outro-off` | `600.0 s`、60 fps、单素材、无 BGM、结尾关闭 | `600.8 s`、1080×1920、60 fps、H.264、音视频流完整、965 个 ASS 事件 | `245.77 s` | 通过 |

稳定性报告位于 `D:\StoryForgeRelease\1.0.8-final\TEST_RESULTS\stability-acceptance.json`，整体耗时 `486.86 s`，编码器为 `libx264`。

## 正式制品

| 制品 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `StoryForge Studio.exe` | `87554537` | `00f9804bdddbfe945a6c9834c28406b51194034358f3fc647b9e697052afbb90` |
| `StoryForge-1.0.8-update.zip` | `776793067` | `a45be3085a71b1c015f577861b831e7268e8961b9267a1da9f610236f6cdf771` |
| `StoryForge-1.0.8-update.zip.manifest.json` | `318` | `4940353dacae8b2b62b24189e9273ded65311811acb848c79ba622470541eb2c` |

- 内部 `storyforge-update.json`：schema `1`、版本 `1.0.8`、入口 `StoryForge Studio.exe`。
- ZIP：`7840` 个条目，解压总字节数 `1736134477`。
- `BUILD_RELEASE_VALIDATION.json`：bundle `7838` 个文件、`1735683236` 字节；bundle manifest SHA-256 `f91ed427ea7ecbc8af907967309f2f894272359145c5d114b4fff1782fd2cb2f`。
- 根目录恢复载荷五项均通过 package smoke：`一键恢复StoryForge-Hub.cmd`、`Restore-StoryForge-Hub.cmd`、`scripts/restore_storyforge_hub_new_machine.ps1`、`scripts/bootstrap_storyforge.ps1`、`scripts/verify_storyforge_deployment.ps1`。

## GitHub 与部署证据

| 项目 | v1.0.8 结果 |
| --- | --- |
| 源码推送 | v1.0.8 制品源码为 `f4ed8bf0052de32736a92429301e86213ed9d983`；`main` 在其后仅增加发布证据文档提交，未修改或重建 v1.0.8 制品 |
| `fast-pr` | [run 31778461124](https://github.com/31-hub-st/Automated-video-editing-tools/actions/runs/31778461124)：`success`；`221` 项测试；`headSha=f4ed8bf0052de32736a92429301e86213ed9d983` |
| 手动 `release-stable-gate` | [run 31779328163](https://github.com/31-hub-st/Automated-video-editing-tools/actions/runs/31779328163)：`success`；`1163` 项完整回归及稳定冻结包门禁通过；同一 `headSha` |
| 注解标签 `v1.0.8` | 标签对象 `77b20b184b7111a490d65e2bc8ff93cef0f9df24` → commit `f4ed8bf0052de32736a92429301e86213ed9d983`；标签消息 `StoryForge Studio v1.0.8` |
| 标签 `release-stable-gate` | [run 31782776480](https://github.com/31-hub-st/Automated-video-editing-tools/actions/runs/31782776480)：`success`；`1163` 项、失败 `0`、错误 `0`、跳过 `2`；两场景稳定门禁 `765.1 s`、package smoke 与五项恢复载荷均通过；证据 artifact ID `9212764722`，`15225` 字节，SHA-256 `4a223417059f5440cfd8cc24a433a0654cff658d5332149b148c05d985d59844`；同一 `headSha` |
| 正式 GitHub Release | `draft=false`、`prerelease=false`、Latest；严格两项资产：ZIP `776793067` 字节 / `sha256:a45be3085a71b1c015f577861b831e7268e8961b9267a1da9f610236f6cdf771`；sidecar `318` 字节 / `sha256:4940353dacae8b2b62b24189e9273ded65311811acb848c79ba622470541eb2c` |
| 发布前 Hub 快照 | 已于 `2026-08-14 08:12 UTC` 刷新；`StoryForge-Hub-Latest.sfbak`：`45619803` 字节，SHA-256 `8b6f7ab4363d0d0b0f6f8acc954a94e9c17a221e70c2254fcc1b372a8ab69eec`；manifest：`679` 字节，SHA-256 `63b43365c218e84703ef822f0668606f7a98858625d603e6c9c37c6c8508b6bc`；资产总数严格为 `2` |
| 生产 Hub 原子升级与健康检查 | `D:\StoryForgeHub\App-1.0.8`；正式验证脚本于 `2026-08-16T19:20:42Z` 返回 `ok=true`，9 项检查全部通过；监听 PID `12292`，Web/RPC 健康版本均为 `1.0.8`，schema `15`，备份状态 `ready`、`operational=true` |
| Worker / 桌面版本与运行路径 | 桌面端实际运行 `D:\StoryForge-1.0.8-update\StoryForge Studio.exe`；本机 Worker 监听 `127.0.0.1:18765`；操作方确认版本已正常使用 |
| Hub 员工更新清单 | `version=1.0.8`；文件 `StoryForge-1.0.8-a45be3085a71.zip`；`776793067` 字节；SHA-256 `a45be3085a71b1c015f577861b831e7268e8961b9267a1da9f610236f6cdf771`；发布时间 `2026-08-14T09:03:34.076258Z` |
| 旧文件清理审计 | GitHub 仅保留 `v1.0.8` 正式 Release 与 `hub-state-latest`；`v1.0.0` 至 `v1.0.6` 的 Release 对象已删除，但同名标签均保留。本机已移除 Hub `App-1.0.7`、1.0.7 构建目录、空更新临时目录及便携 1.0.7 程序文件；清理采用 Windows 回收站以保持可恢复性，未清空回收站。所有 `StoryForgeData`、Hub `Data`、恢复数据库及 `D:\StoryForgeRelease\1.0.8-final` 均保留 |

本次发布后收尾只补充文档、校准本地标签并清理旧程序载荷；没有移动远端标签、重建同版本制品、修改已验证 ZIP 字节或手工改动任何 SQLite。
